# Dockerfile for production (no nodemon)
FROM node:18-alpine

# Set working directory
WORKDIR /app

# Copy everything
COPY . .

# Install only production dependencies
RUN npm install --production

# Expose app port (if you're using 3000 or change accordingly)
EXPOSE 3000

# Start the app (NO nodemon)
CMD ["node", "src/index.js"]
